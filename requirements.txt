# StorySprout — DeepSeek (text) + Gemini (images/QA) + GCS (image bytes) + Firestore (JSON store)
google-genai>=1.0.0
google-cloud-storage>=2.16.0
google-cloud-firestore>=2.16.0

# Image handling
Pillow>=10.2.0

# Renderer
reportlab>=4.1.0

# Backend
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9
pydantic>=2.6.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.27.0
tqdm>=4.66.0
